# Mobile instructions

This module is a React Native / Expo app.

Use:
- TypeScript
- Expo
- typed API client
- small components
- separate API logic from UI screens

Do not hardcode backend URLs inside screens.
After changes, run npm run lint and npm test if available.
